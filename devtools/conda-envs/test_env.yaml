name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python >=3.10,<3.12
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # databases
  - pandas
  - bravado
  - requests-cache
  - beautifulsoup4
  - numpy
  - biopython
  - pydantic
  - GitPython
  - importlib-resources

    # schema
  - gitpython
  - pydantic
  - strenum
  - toml
  - pyyaml
  - tqdm
  # - pip:
  #   - git+https://github.com/tansey-lab/nf-rnaseq
