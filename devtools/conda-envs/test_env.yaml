name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python >=3.10,<3.12
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Databases
  - pandas
  - bravado
  - requests-cache
  - beautifulsoup4
  - numpy
  - biopython
  - pydantic
  - GitPython
  - importlib-resources

    # schema
  - gitpython
  - pydantic
  - strenum
  - toml
  - pyyaml
  - tqdm
