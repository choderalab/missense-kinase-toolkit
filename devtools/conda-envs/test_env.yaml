name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Databases
  - pandas
  - bravado
  - requests-cache
  - beautifulsoup4
  - numpy
  - biopython
