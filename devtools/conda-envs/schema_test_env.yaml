name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # schema
  - gitpython==3.1.43
  - pydantic==2.10.3
  - strenum==0.4.15
  - toml
  - pyyaml
